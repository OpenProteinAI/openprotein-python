# api.embedding.EmbeddingResultFuture { #openprotein.api.embedding.EmbeddingResultFuture }

`api.embedding.EmbeddingResultFuture(self, session, job, sequences=None, max_workers=config.MAX_CONCURRENT_WORKERS)`

Future Job for manipulating results

## Methods

| Name | Description |
| --- | --- |
| [get_item](#openprotein.api.embedding.EmbeddingResultFuture.get_item) | Get embedding results for specified sequence. |

### get_item { #openprotein.api.embedding.EmbeddingResultFuture.get_item }

`api.embedding.EmbeddingResultFuture.get_item(self, sequence)`

Get embedding results for specified sequence.

Args:
    sequence (bytes): sequence to fetch results for

Returns:
    np.ndarray: embeddings